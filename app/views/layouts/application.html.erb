<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% if TradeSetting.company == 'dulux' %>
    <title>Magic - 多乐士电商ERP BETA 1.0</title>
    <% else %>
    <title><%= content_for?(:title) ? yield(:title) : "Magic Orders" %></title>
    <% end %>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <%= link_to image_tag("logo.png"), root_path, class: 'brand' %>
          <% if user_signed_in? -%>

          <div class="btns btn-group pull-right" style="min-width:120px;">
            <button class="btn btn-primary btn_mar4"><i class="icon-user icon-white"></i> <%= current_user.display_name %></button>
            <button data-toggle="dropdown" class="btn btn-primary dropdown-toggle"><span class="caret"></span></button>
            <ul class="dropdown-menu">
              <li><%= link_to "注销", destroy_user_session_path, method: :delete %></li>
            </ul>
          </div>

          <ul class="nav nav-pills" id="top-nav">
            <li class="active trades" style="display:none"><a href="#">订单管理</a></li>
            <li class="trades" style="display:none"><a href="http://shop.nipponpaint.com.cn/admin/orders">官网订单</a></li>
            <li class="trades" style="display:none"><a href="http://shop.nipponpaint.com.cn/admin/taobao_orders">淘宝订单</a></li>
            <li class="areas" style="display:none"><a href="#">区域管理</a></li>
            <li class="sellers" style="display:none"><a href="#">经销商管理</a></li>
            <% if current_user.role_key == 'admin' || current_user.role_key == 'seller'%>
            <li class="stocks" style="display:none"><a href="#">仓库管理</a></li>
            <% end %>
            <% if current_user.role_key == 'admin' %>
            <li class="products" style="display:none"><a href="#">商品管理</a></li>
            <li class="users" style="display:none"><a href="#">系统管理</a></li>
            <% end %>
          </ul>

          <ul class="nav pull-right">
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Magic<b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><a href="/trades" class="trades">订单管理</a></li>
                <li><a href="/sellers" class="sellers">经销商管理</a></li>
                <% if (current_user.role_key == 'admin' || current_user.role_key == 'seller') && current_user.seller_id.present? %>
                <li><%= link_to '仓库管理', seller_stocks_path(current_user.seller_id), class: "stocks" %></li>
                <% end %>
                <% if current_user.role_key == 'admin' %>
                <li><%= link_to '商品管理', products_path, class: "products" %></li>
                <li class= "divider"></li>
                <li><a href="/users" class="users">系统设置</a></li>
                <% end %>
              </ul>
            </li>
            <li class="divider-vertical"></li>
          </ul>
          <!--/.nav-collapse -->

          <% end %>
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <%= yield %>

      <footer>
        <p>&copy; Doorder/NIO team 2012</p>
      </footer>
    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <script type="text/javascript">

      $(document).ready(function(e) {

        $(function(){
         $('#myModal').modal('toggle');
         $('#myModal').on('shown',function(){
           $('.modal-backdrop').unbind('click');
         });
       });

      });
    </script>
    <%= yield :tail -%>
  </body>
</html>
