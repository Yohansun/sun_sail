<%= simple_form_for(@third_party) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

  <div class="form-inputs">
    <%= f.input :is_default %>
  </div>

  <div class="form-actions">
    <%= f.button :submit,"提交" %>
  </div>
<% end %>
