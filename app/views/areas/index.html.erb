<div class="container-fluid">
  <div class="row-fluid">
    <div class="span2 bs-docs-sidebar">
      <ul class="nav nav-list bs-docs-sidenav affix span2">
        <li><a href="/users">用户管理</a></li>
        <li class="active">
          <a href="/areas" class="" data-toggle="collapse" data-target="#autoset_menu"><i class="icon-chevron-right"></i>地域管理</a>
        </li>
        <li><a href="/logistics">物流商管理</a></li>
        <% if current_account.settings.enable_module_onsite_service == 1  %>
          <li><a href="/onsite_services">上门服务</a></li>
        <%- end %>
        <% if current_account.settings.enable_module_logistic_group == 1 %>
          <li><a href="/logistic_groups">物流分组管理</a></li>
        <%- end %>
      </ul>
    </div>
    <div class="span10">
      <div class="row-fluid">
        <ul class="breadcrumb" style="position:relative;">
          <li><a href="/">Magic</a><span class="divider">/</span></li>
          <li class="active">地域管理</li>
        </ul><!-- breadcrumb -->
        <table class="table table-striped table-bordered table-condensed td_colors mar_t">
          <thead>
            <tr>
              <th>ID</th>
              <th>邮编</th>
              <th>名称</th>
              <th>默认经销商</th>
              <th>操作</th>
            </tr>
          </thead>
          <tbody>
            <% @areas.each do |area| %>
              <tr>
                <td><%= area.id %></td>
                <td><%= area.zip %></td>
                <td><%= area.name %></td>
                <td>
                <% if area.children.count == 0 %>
                <%= area.sellers.try(:map){|s| s.name}.try(:join, ",") %>
                <% else %>
                <%= area.children.count %>个下级经销商
                <% end %>
                </td>
                <td>
                  <% if area.children.count > 0 %>
                  <div class="btn-group">
                    <button data-toggle="dropdown" class="btn btn-info dropdown-toggle min_w"><i class="icon-wrench"></i> <span class="caret"></span></button>
                    <ul class="dropdown-menu" style="left:-115px;">
                      <li><%= link_to "查看下级", areas_path(:parent_id => area.id), :class=>"browse_children"%></li>
                    </ul>
                  </div>
                  <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

