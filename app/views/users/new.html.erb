<%
  breadcrumb << ["系统设置", users_path]
  breadcrumb << ["用户管理", users_path]
  breadcrumb << ["新增用户"]
%>
<div class="row-fluid">
  <div class="span10 w100">
    <ul class="nav nav-tabs">
      <li class="active"><a href="/users/new">新增用户</a></li>
    </ul><!-- nav-tabs -->
    <div class="tab-content">
      <%= simple_form_for @user, html: {:class =>'well form-horizontal'} do |f| %>
        <%= render :partial => 'form',:locals => {:f => f} %>
      <% end %>
    </div>
  </div>
</div>