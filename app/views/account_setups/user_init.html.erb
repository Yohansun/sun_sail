<body class='whiteSmoke_bg'>
  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar">
      <div class="navbar-inner">
        <div class="container-fluid">
          <%= link_to image_tag("logo.png"), 'javascript:void(0)', class: 'brand' %>
        </div>
      </div>
    </div>
  </div>
  <div class="container-fluid start_key">
    <div class="row-fluid">
      <h4 class="pagination-centered start_tit">亲，工作不要太累，找几个人跟你一起开店吧！</h4>
      <ul class="unstyled pagination-centered clearfix start_tips start_tips4">
        <li class="pull-left" style="width:25%;">管理员设置</li>
        <li class="pull-left" style="width:27%;">店铺初始化</li>
        <li class="pull-left" style="width:25%;">店铺功能设置</li>
        <li class="active pull-left" style="width:20%;">店铺账号设置</li>
      </ul>
      <%= simple_form_for User.new, url: account_setup_path, method: :put, html: { class: 'form-horizontal start_form3'} do |f| %>
        <div class="clearfix">
          <div class="well span8 offset2">
            <ul class="thumbnails">
              <li class="span6">
                <div class="thumbnail">
                  <img src="/images/account_setups/start_key4_1.jpg" alt="">
                  <h5 class="" style="background:#e6b831;">客服账号设置</h5>
                  <div align="left" class="clearfix control-group <%= 'error' if @cs.present? && @cs.errors[:username].present? %>">
                    <%= text_field_tag "cs[username]", params[:cs].is_a?(Hash) ? params[:cs][:username] : "", class: "pagination-centered input-xlarge", placeholder: "请输入客服账号的登录名" %>
                    <small class="muted">
                      <%= if @cs.present? && @cs.errors[:username].present?
                          @cs.errors[:username].first
                        else
                          "* 3-12个字符"
                        end
                      %>
                    </small>
                  </div>
                  <div align="left" class="clearfix control-group <%= 'error' if @cs.present? && @cs.errors[:email].present? %>">
                    <%= text_field_tag "cs[email]", params[:cs].is_a?(Hash) ? params[:cs][:email] : "", class: "pagination-centered input-xlarge", placeholder: "请输入客服账号的邮箱" %>
                    <small class="muted">
                      <%= if @cs.present? && @cs.errors[:email].present?
                          @cs.errors[:email].first
                        else
                          "*请确保输入的邮箱正确"
                        end
                      %>
                    </small>
                  </div>
                  <div align="left" class="clearfix control-group <%= 'error' if @cs.present? && @cs.errors[:phone].present? %>">
                    <%= text_field_tag "cs[phone]", params[:cs].is_a?(Hash) ? params[:cs][:phone] : "", class: "pagination-centered input-xlarge", placeholder: "请输入客服账号的手机" %>
                    <small class="muted">
                      <%= @cs.errors[:phone].first if @cs.present? && @cs.errors[:phone].present? %>
                    </small>
                  </div
                </div>
              </li>
              <li class="span6">

                <div class="thumbnail">
                  <img src="/images/account_setups/start_key4_2.jpg" alt="">
                  <h5 class="">仓库管理员账号设置</h5>
                  <div align="left" class="clearfix control-group <%= 'error' if @stock_admin.present? && @stock_admin.errors[:username].present? %>">
                    <%= text_field_tag "stock_admin[username]", params[:stock_admin].is_a?(Hash) ? params[:stock_admin][:username] : "", class: "pagination-centered input-xlarge", placeholder: "请输入仓库管理员的账号名" %>
                    <small class="muted">
                      <%= if @stock_admin.present? && @stock_admin.errors[:username].present?
                          @stock_admin.errors[:username].first
                        else
                          "* 3-12个字符"
                        end
                      %>
                    </small>
                  </div>
                  <div align="left" class="clearfix control-group <%= 'error' if @stock_admin.present? && @stock_admin.errors[:email].present? %>">
                    <%= text_field_tag "stock_admin[email]", params[:stock_admin].is_a?(Hash) ? params[:stock_admin][:email] : "", class: "pagination-centered input-xlarge", placeholder: "请输入仓库管理账号的邮箱" %>
                    <small class="muted">
                      <%= if @stock_admin.present? && @stock_admin.errors[:email].present?
                          @stock_admin.errors[:email].first
                        else
                          "*请确保输入的邮箱正确"
                        end
                      %>
                    </small>
                  </div>
                  <div align="left" class="clearfix control-group <%= 'error' if @stock_admin.present? && @stock_admin.errors[:phone].present? %>">
                    <%= text_field_tag "stock_admin[phone]", params[:stock_admin].is_a?(Hash) ? params[:stock_admin][:phone] : "", class: "pagination-centered input-xlarge", placeholder: "请输入仓库管理账号的手机" %>
                    <small class="muted">
                      <%= @stock_admin.errors[:phone].first if @stock_admin.present? && @stock_admin.errors[:phone].present? %>
                    </small>
                  </div>
                </div>
              </li>
            </ul>
            <div class="pagination-centered" style="color:#dc9b17;">
              系统将发送密码邮件到您输入的邮箱和手机，请确保邮箱和手机无误。
            </div>
          </div>
        </div>
        <div class="pagination-centered">
          <%= f.button :submit, '开始使用', :class=>"btn btn-success", :"data-disable-with" => '提交中...' %>
        </div>
      <%- end -%>
    </div>
  </div>
</body>