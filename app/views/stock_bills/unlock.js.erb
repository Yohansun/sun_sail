<%- @stock_bills.each do |bill|%>
$("#status_<%= bill.id %>").replaceWith("<%= j render('status', bill: bill) %>")
<%- end %>
<% if @messages.present? %>
  alert("<%= @messages.join(';') %>")
<% end %>